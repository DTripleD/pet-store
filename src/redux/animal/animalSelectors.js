export const selectAnimals = (state) => state.animal.allAnimals;

export const selectCurrentAnimal = (state) => state.animal.currentAnimal;

export const selectAnimalsLoading = (state) => state.animal.isLoading;
